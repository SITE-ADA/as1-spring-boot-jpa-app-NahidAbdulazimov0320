CREATE TABLE STUDENT (
    id INT PRIMARY KEY,
    name VARCHAR(255),
   age INT,
   faculty VARCHAR(255)


);

CREATE TABLE COURSE (
    course_id INT PRIMARY KEY,
    course_name VARCHAR(255),
    professor VARCHAR(255)
);